import { DeclarationListEmitMode } from "@angular/compiler";

export interface Playlist {
    PlaylistID: number;
    UserID: number;
    user: string;
    SongCount: number;
    Timeframe: number;
    /**Date: datetime;**/
}
